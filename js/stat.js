var CLOUD_WIDTH = 420;var CLOUD_HEIGHT = 270;var CLOUD_X = 100;var CLOUD_Y = 10;var GAP = 10;var TEXT_GAP = 50;var BAR_WIDTH = 40;var BAR_HEIGHT = 150;var BAR_DIS = 50;var TEXT_START = CLOUD_X + GAP + GAP + GAP;var BAR_START_X = CLOUD_X + GAP + GAP + GAP;var BAR_START_Y = CLOUD_Y * 8;var renderCloud = function(ctx, x, y, color) {    ctx.fillStyle = color;    ctx.fillRect(x, y, CLOUD_WIDTH, CLOUD_HEIGHT);    ctx.fillStyle = '#000';    ctx.font = '16px PT Mono';    ctx.textBaseline = 'hanging';    ctx.fillText('Ура вы победили!', 240, 20);    ctx.fillText('Список результатов:', 225, 40);};var getMaxElement = function(arr) {    var maxElement = arr[0];    for (var i = 0; i < arr.length; i++) {        if (arr[i] > maxElement) {            maxElement = arr[i];        }    }    return maxElement;};window.renderStatistics = function(ctx, names, times) {    renderCloud(ctx, CLOUD_X + GAP, CLOUD_Y + GAP, 'rgba(0, 0, 0, 0.7)');    renderCloud(ctx, CLOUD_X, CLOUD_Y, '#fff');    ctx.fillStyle = 'rgba(255, 0, 0, 1)';    var maxTime = getMaxElement(times);    for (var i = 0; i < names.length; i++) {        ctx.fillText(names[i], TEXT_START + (BAR_WIDTH + BAR_DIS) * i, CLOUD_Y + TEXT_GAP);        ctx.fillRect(BAR_START_X + (BAR_WIDTH + BAR_DIS) * i, BAR_START_Y, BAR_WIDTH, (BAR_HEIGHT * times[i]) / maxTime);        ctx.fillStyle = 'blue';    };};